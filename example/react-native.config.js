module.exports = {
  dependencies: {
    'react-native-s-baidumap': {
      // root: '/lib/react-native-s-baidumap/lib',
      platforms: {
        ios: null, // disable ios platform, other platforms will still autolink if provided
      },
    },
  },
};
